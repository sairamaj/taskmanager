<UserControl x:Class="Demo1.Views.TaskDemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Demo1.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="This is Sample demo task1" />
        <TextBlock Text="{Binding DemoName}" />
        <Button Content="Log Debug" Command="{Binding LogDebugCommand}" Width="80"/>
        <Button Content="Log Error" Command="{Binding LogErrorCommand}" Width="80"/>
        <Button Content="Log Info" Command="{Binding LogInfoCommand}" Width="80"/>
    </StackPanel>
</UserControl>